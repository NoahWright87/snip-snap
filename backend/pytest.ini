[pytest]
# ml/tests requires the optional deps in ml/requirements.txt (torch,
# open_clip, ...) and is not part of the app's default test suite - run it
# explicitly with `pytest ml/tests` after installing those.
testpaths = tests
