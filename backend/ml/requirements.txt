# Phase 1 of the auto-detection roadmap (see GitHub issue #3): CLIP embedding
# extraction + caching from manually tagged segments. Kept separate from
# ../requirements.txt so the packaged app isn't affected until this ships -
# torch/open_clip are large and CUDA-optional, ffmpeg-style large-download
# concerns don't apply since these come from pip, but the install footprint
# is still much bigger than the rest of the app's deps.
torch>=2.2
open_clip_torch>=2.24
numpy>=1.26
scikit-learn>=1.4
Pillow>=10.2
